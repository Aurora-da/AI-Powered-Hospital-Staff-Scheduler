services:
  app:
    build: .
    container_name: schedule-assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./agent/data:/app/agent/data
    restart: unless-stopped
