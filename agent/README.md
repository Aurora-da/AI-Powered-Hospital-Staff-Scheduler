# 医院智能排班与调班智能体

使用 Python 3.10+、OR-Tools（CP-SAT）、pandas、openpyxl 实现的简易示例项目。

## 安装依赖

```bash
pip install -r requirements.txt
```

## 运行方式

在项目根目录执行：

```bash
python main.py
```

随后根据提示输入自然语言，例如：

```text
张医生10月3号夜班想调班
```

